import styles from './index.module.sass';

const ProjectList = () => {
	return (
		<section className={styles.projectList}>
			<div className='section__title projectList__title'>Portfolio</div>
			<div className='projectList__slider'></div>
		</section>
	);
};

export default ProjectList;
